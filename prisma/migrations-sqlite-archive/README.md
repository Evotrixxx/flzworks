# Archived SQLite migrations

These migrations describe the legacy SQLite database and are retained for
recovery and audit purposes. They must not be run against PostgreSQL.

Production migrations live in `prisma/migrations`, beginning with the clean
PostgreSQL baseline.
