Pill input on glass. Labels are uppercase mono, sitting above the field.

```jsx
<Input label="Email" placeholder="you@studio.com" icon={<Glyph name="circle" size={13} />} />
```

Focus is a 3px warm-accent ring, never a border-colour swap.
