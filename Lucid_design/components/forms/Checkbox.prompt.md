Glass checkbox / radio (`round`). Checked state is the ink capsule with a 1.8px check.

```jsx
<Checkbox checked={a} onChange={setA} label="Remember this device" />
<Checkbox round checked={plan === 'pro'} onChange={() => setPlan('pro')} label="Pro" />
```
