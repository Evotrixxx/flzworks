Detached vertical icon rail for app shells.

```jsx
<IconRail items={[{id:'home',icon:'home'},{id:'pay',icon:'wallet'}]} value={v} onChange={setV} tone="dark" />
```

Fully pill-shaped, floats away from the viewport edge (24–40px), icon-only with aria labels.
