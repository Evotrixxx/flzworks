Filter chip row (each chip is its own capsule).

```jsx
<Segmented items={['All','Salary','Software','Rent']} value={f} onChange={setF} tone="dark" />
```

Pair with a trailing `<IconButton name="plus" />` when the set is user-extendable.
