Big-number metric. The superscript unit is a signature detail — use it for `$`, `%`, ordinals.

```jsx
<StatBlock label="Overall balance" value="990,815" unit="$" size="lg" tone="dark" />
```

Numbers are display-weight with -0.03em tracking; labels always mono uppercase above them.
