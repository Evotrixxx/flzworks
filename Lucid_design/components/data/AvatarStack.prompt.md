Overlapping avatar row (30% overlap, 2px surface-coloured ring).

```jsx
<AvatarStack people={['Ada L','Sam O','Rae K']} onAdd={invite} tone="dark" />
```

Initials on flat accent fills stand in for photos — this system ships no portrait assets.
